// Object
let info = {
    
    username: 'mbrsagor',
    age: 26,
    address: 'Dhaka, Bangladesh'
}
// console.log(info);
for (const [key, val] of Object.entries(info)) {
    console.log(`${key} : ${val}`);
}

// Splete 